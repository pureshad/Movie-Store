@model IEnumerable<Vidly001.Models.Entity.Customer>

@{
    ViewBag.Title = "CustomersMenu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customer Menu</h2>

<div class="container">
    <div class="form-group row">
        <table class="table table-bordered">
            <tr>
                <th>Customer Name</th>
                @*<th>Discount Rate</th>*@
                <th>Membership Type</th>
            </tr>
            @foreach (var customer in Model)
            {
            <tr>
                <td>@Html.ActionLink(customer.Name, "Details", "Customer", new { id = customer.Id }, null)</td>
                @*<td>@customer.MembershipType.DiscountRate%</td>*@
                <td>@customer.MembershipType.SubscriptionTypeName</td>
            </tr>
            }
        </table>
    </div>
</div>